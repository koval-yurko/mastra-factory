---
title: 'Narrow engines.node to 22|24 and correct four AGENTS.md managed-block claims'
type: 'chore'
created: '2026-09-25'
status: 'done'
baseline_revision: '521ddb667f96902661204a92a0b98fe4d53319ea'
review_loop_iteration: 0
followup_review_recommended: false
context: []
warnings: ['multiple-goals', 'oversized']
deferred:
  - summary: >-
      Nothing in the verify gate reads `engines`, and `EBADENGINE` stays warn-only, so the narrowed
      Node range is declared but never observed or enforced.
    evidence: |-
      `engines` appears zero times in `.bmad-loop/policy.toml`; the only entry that parses the
      manifest (the gate-script census) reads `scripts` and never touches `engines`. There is no
      `.npmrc` at the repo root and `npm config get engine-strict` is `false`, so `npm ci` exits 0
      on an excluded major. Demonstrated: reverting `engines.node` to `">=22.19.0"`, or mistyping
      it as `"^22.19.0 || ^25.0.0"`, leaves every gate entry green; a scratch project declaring
      `"^20.0.0"` runs `npm ci --no-audit --no-fund` to exit 0 with only an `EBADENGINE` warning.
      This is DW-10's stated mechanism ("the gate would run its test runner on an unsupported
      runtime"), which the recorded 2026-09-24 decision addressed with a declaration rather than an
      enforcement mechanism. Closing it means appending one `[verify].commands` entry that asserts
      the fixed table (22.19.0 accepted, 23.11.0 rejected, 24.19.0 accepted, 25.0.0 rejected) and
      that `process.version` satisfies the declared range — which this bundle's scope forbids.
      Related: `spec-package-json-declaration-hygiene.md` records a standing stance against adding
      `engine-strict` or an `.npmrc`, so the enforcement half needs a human decision, not a guard.
    location: >-
      package.json:41 / .bmad-loop/policy.toml [verify].commands
    severity: medium
  - summary: >-
      The Node fact is now stated in four artifacts with no arm tying any two together, so the
      freshly corrected AGENTS.md claim can drift again exactly the way DW-3 and DW-65 did.
    evidence: |-
      After this change the contract appears at `package.json:41`, `package-lock.json:33`,
      `AGENTS.md:10` ("Node 22.19+ or 24") and `ops/factory-start.sh:20` (`v24.19.0`, exact), plus
      the diagram at `docs/self-hosting-research.md:61`. Guards 53, 54 and 55 are the only entries
      that open AGENTS.md; none of them parses `engines`. Demonstrated: narrowing `engines.node` to
      `"^24.0.0"` alone and leaving AGENTS.md untouched keeps all 63 entries green while the block
      keeps telling agents "Node 22.19+ or 24". One appended guard could derive the admitted majors
      from `engines.node` and require the block's summary sentence to name exactly those.
    location: >-
      AGENTS.md:10
    severity: medium
  - summary: >-
      The "only check" claim class this pass removed by hand is still caught by nothing, and the
      managed block is regenerated by bmad-project-context, so all four corrections can revert.
    evidence: |-
      DW-95 already records that guard 53's denial arm matches `no <script> script`, not an "only
      check" assertion, and re-measured green here: guards 53/54/55 exit 0 on both the old and the
      new wording of `AGENTS.md:17`, and on both the old and the new `npm ci` prose. The repo's own
      doctrine at `.bmad-loop/policy.toml:1991-1993` is that a hand patch inside the managed block
      is replaced on the next refresh while "a guard is not replaced", and `policy.toml:2010-2022`
      describes the one-story lag that follows. The intent's closing note ("every edit has to
      survive a refresh") is therefore satisfied only in the sense that the edits pass today's gate;
      nothing makes them durable. The array is append-only and this bundle's scope forbids
      appending, so the durable fix is a later story or a bmad-project-context post-write check.
    location: >-
      AGENTS.md:14-19 and :52-60
    severity: medium
  - summary: >-
      `@types/node` is pinned to the 22 major while `engines.node` now declares 24 supported, so
      `tsc --noEmit` type-checks against Node 22 typings on either runtime.
    evidence: |-
      `package.json` devDependencies pin `"@types/node": "22.20.1"`. With 24 declared supported, the
      gate cannot see 24-only API surface as valid and would reject it on a supported runtime.
      Nothing in the change or the gate ties the typings major to `engines.node`.
    location: >-
      package.json:33
    severity: low
  - summary: >-
      npm does not validate the lockfile's root `engines` mirror against `package.json`, so a future
      manifest-only edit leaves the two silently out of step.
    evidence: |-
      Demonstrated on this machine (npm 11.17.0): a scratch project whose lock declares
      `packages[""].engines.node: ">=99.0.0"` while `package.json` declares `"^22.19.0 || ^24.0.0"`
      runs `npm ci --no-audit --no-fund` to exit 0 with no warning and no lock-mismatch error. This
      change regenerated the mirror correctly, but the "confirm the diff is confined to that one
      line" step is human-run and nothing repeats it.
    location: >-
      package-lock.json:33
    severity: low
  - summary: >-
      Several installed packages declare Node ranges narrower than the new `engines.node`, so
      `EBADENGINE` warnings remain reachable inside the two admitted majors.
    evidence: |-
      Read from `package-lock.json`: `posthog-node` declares `^20.20.0 || >=22.22.0` (so 22.19.0
      through 22.21.x warn), the 27 `@babel/*` packages declare `^22.18.0 || >=24.11.0` (so 24.0.0
      through 24.10.x warn), and `@napi-rs/lzma-linux-x64-gnu` declares `^22.20 || ^24.12 || >=25`.
      Pre-existing — `">=22.19.0"` admitted all of these too — and warn-only either way, but it
      means the declared range is not the intersection of what the tree actually supports.
    location: >-
      package.json:41
    severity: low
  - summary: >-
      No operator-facing document states a Node prerequisite, so the human installing this on a
      fresh Mac never reads the majors this change decided.
    evidence: |-
      Root `README.md`, `docs/`, `ops/README.md` and `sandbox/README.md` carry no Node version
      statement; the version appears only in `ops/factory-start.sh:20`'s `NODE_BIN` pin, the
      `docs/self-hosting-research.md:61` diagram, and now `package.json` and the AGENTS.md block —
      none of which is the setup path. Editing `README.md` adds no root file, so AD-3 does not block
      it; it is simply outside this bundle's two named files.
    location: >-
      README.md
    severity: low
---

<intent-contract>

## Intent

**Problem:** `package.json` declares `engines.node: ">=22.19.0"`, an open upper bound that admits Node 23 and 25 — majors `vitest@5.0.1` does not support (`^22.12.0 || ^24.0.0 || >=26.0.0`), and npm reports the mismatch as a warning only (DW-3, DW-10, DW-101). Four claims inside AGENTS.md's `<!-- bmad:context -->` managed block are stale or false: line 9 says "TypeScript on Node 22" while the machine and `ops/factory-start.sh` run 24 (DW-3, DW-65); line 16 says "`tsc --noEmit` is the only real check, so code outside `src/` is silently unverified", which is false against 63 `[verify].commands` entries that check `ops/`, `sandbox/`, the environment-key split and this block's own prose (DW-95); line 52 copies entry 1's unguarded `--no-audit --no-fund` flags into prose (DW-94); and line 2 runs to 120 columns against the block's ~100-column wrap and stamps `47966e45abbd`, the baseline, which predates the two commits that wrote the block (DW-94, DW-97).

**Approach:** Two files, five one-place edits, then the full verify gate. Narrow `engines.node` to `"^22.19.0 || ^24.0.0"` (regenerating the lockfile's root `engines` mirror), and correct the four AGENTS.md claims in place inside the managed block.

## Boundaries & Constraints

**Always:** Keep every AGENTS.md edit inside the `<!-- bmad:context -->` … `<!-- /bmad:context -->` markers and hard-wrapped at about 100 columns, matching the rest of the block. Keep `` `.bmad-loop/policy.toml` is tracked `` on a single physical line — guard 54's positive arm matches the path followed by at most a few non-alphanumeric characters and then `is tracked`, so a line wrap between them breaks it. Keep the `## Running and verifying` first column-0 bullet naming `npm run check`, `npm test` and `npm run build` (guard 53's first-bullet arm). Word every rewrite so it carries no `no <script-name>` denial (guard 53's denial arm allows up to three intervening words) and no count word immediately before guard/guards/command/commands/check/checks/entry/entries (guard 53's count arm). Restamp the provenance line with a full 40-character SHA whose commit contains the managed block. Keep `package.json` and `package-lock.json` in sync.

**Never:** Do not touch `ops/factory-start.sh`'s `NODE_BIN=…/v24.19.0` pin or `docs/self-hosting-research.md:61`'s "Factory Server (one Node 24)" — the recorded decision makes both accurate once AGENTS.md names 24. Do not change `sandbox/factory-sandbox.Dockerfile`'s `node:22-bookworm-slim` base (22 stays supported). Do not add `.nvmrc` or any other root-level file (AD-3: root is a closed set). Do not edit, reorder or append to `[verify].commands`. Do not edit the deferred-work ledger or the sprint board. Do not restate the gate array's length anywhere.

</intent-contract>

## Code Map

- `package.json:40-43` -- `engines` block. `"node": ">=22.19.0"` is the line to narrow; `"npm": ">=11.17.0"` stays. Declared scripts (read by guard 53): `dev`, `db:up`, `db:down`, `check`, `test`, `build`, `start`, `deploy`.
- `package-lock.json:32-35` -- the lockfile's root `packages[""].engines` mirror of the same two lines. Regenerate rather than hand-edit.
- `node_modules/vitest/package.json` (lock line 3808 region) -- `vitest@5.0.1` declares `engines.node: "^22.12.0 || ^24.0.0 || >=26.0.0"`; this is the constraint that excludes 23 and 25.
- `AGENTS.md:1` -- `<!-- bmad:context -->` opening marker. Guards 53, 54 and 55 all clamp to this marker and `AGENTS.md:89`'s `<!-- /bmad:context -->`; a missing or inverted marker is an extraction failure, not a pass.
- `AGENTS.md:2-3` -- provenance comment. Line 2 is 120 columns and stamps `47966e45abbdc437359f20f69fd5492a6e933cdb`. The replacement SHA cannot be `HEAD` at authoring time: that tree still carries the claims this change corrects, which is exactly DW-97's defect. It is `4c176b4f84cdc16cd322c840c0d10d3d27d44596` — the commit that lands the corrected block, stamped into a second commit on top of it, so the stamped tree carries the verified block.
- `AGENTS.md:9` -- "TypeScript on Node 22, npm, one package." inside the `## mastra-factory` summary paragraph.
- `AGENTS.md:15-18` -- first `## Policy` bullet; line 16 carries the false "only real check" clause.
- `AGENTS.md:47-62` -- `## Running and verifying`, the section guard 53 censuses. Line 49-50 is the first column-0 bullet (must keep naming the three gated scripts). Lines 51-59 are the gate bullet; line 52 carries the copied flags. Line 59 ends with `` `.bmad-loop/policy.toml` is tracked, so a story worktree has it. ``
- `.bmad-loop/policy.toml:3205-3269` -- `[verify].commands`, 63 entries, one per line. Entry 1 is `"npm ci --no-audit --no-fund"`. Entries 53, 54 and 55 (lines 3257-3259 region, the three `node -e` bodies naming `AGENTS.md`) are the guards that read this block. Read-only for this change.
- `ops/factory-start.sh:20` and `docs/self-hosting-research.md:61` -- read-only evidence that Node 24 is what actually runs; both are left as they are.

## Tasks & Acceptance

**Execution:**
- `package.json` -- change `engines.node` from `">=22.19.0"` to `"^22.19.0 || ^24.0.0"` -- admits only the two majors `vitest@5.0.1` supports and the repo actually runs, closing DW-3/DW-10/DW-101.
- `package-lock.json` -- regenerate the root `engines` mirror with `npm install --package-lock-only --no-audit --no-fund`, then confirm the diff is confined to that one line -- the lockfile is the other half of "a fresh worktree resolves identically".
- `AGENTS.md:9` -- change "TypeScript on Node 22" to name both supported majors -- makes the block agree with `engines.node`, with `ops/factory-start.sh`'s 24 pin and with `docs/self-hosting-research.md`'s "one Node 24" (DW-3, DW-65).
- `AGENTS.md:16` -- reword the "only real check" clause to state what `tsc --noEmit` does not cover instead of what nothing else checks -- the claim is false against the rest of the gate (DW-95).
- `AGENTS.md:52` -- drop `--no-audit --no-fund` from the prose, leaving `` `npm ci` `` -- prose that copies an entry's flags goes stale when the entry changes (DW-94).
- `AGENTS.md:2-3` -- rewrap the provenance comment to about 100 columns and restamp it with the SHA of the commit that lands the corrected block, in a follow-up commit -- so the next refresh diffs against a tree that actually has the verified block (DW-94, DW-97).

**Acceptance Criteria:**
- Given `package.json` after the change, when its `engines.node` range is evaluated against 22.19.0, 23.11.0, 24.19.0 and 25.0.0, then 22.19.0 and 24.19.0 satisfy it and 23.11.0 and 25.0.0 do not.
- Given the repository after the change, when `npm ci --no-audit --no-fund` runs on Node 24.19.0, then it completes successfully and prints no `EBADENGINE` warning naming the root package.
- Given `AGENTS.md` after the change, when the managed block is read, then every line this change touches is at most 100 characters wide, no line is left wider than it was at `baseline_revision`, and the only line still over 100 is the architecture-spine citation (`AGENTS.md:43` at baseline, `:44` after the provenance rewrap adds a line), whose overflow is a single unbreakable path token and which this change does not touch.
- Given `AGENTS.md` after the change, when the managed block is read, then it names both supported Node majors and contains neither the string `--no-audit` nor a clause asserting that any one command is the only check.
- Given `AGENTS.md:2` after the change, when the stamped SHA is resolved, then that commit's `AGENTS.md` is byte-identical to the working tree's apart from the provenance line itself, and `git log --diff-filter=DR --name-only <sha>..HEAD` — the scan `bmad-project-context`'s refresh runs from the stamped SHA — reports nothing.
- Given the repository after the change, when `[verify].commands` entry 1 is read, then it still carries `--no-audit --no-fund` — the prose was loosened, the command was not.
- Given the whole repository after the change, when every entry of `[verify].commands` runs in order, then all 63 exit 0 — guards 53, 54 and 55 in particular.

## Spec Change Log

### 2026-09-25 — Planning correction, before review
- Triggering observation: the first-pass acceptance criterion required that **no** line in the managed block exceed 100 columns. That was never the block's convention — at `baseline_revision` lines 2 (120), 43 (109) and 56 (101) were already over, and the bundle intent describes the wrap as "~100". The implementation subagent honoured the literal criterion by reflowing two pre-existing lines it had no other reason to touch.
- Amended: the wrap criterion now reads "every line this change touches is at most 100 characters wide, no line is left wider than it was at `baseline_revision`, and the only line still over 100 is the architecture-spine citation, whose overflow is a single unbreakable path token". The byte-based `awk` verification command was replaced with a character-based scan, because macOS `awk` counts bytes and flagged a 99-character line carrying an em dash.
- Known-bad state avoided: splitting the architecture-spine path across a backtick boundary, and reflowing a `WORKOS_COOKIE_PASSWORD` line that was never over 100 characters — two edits outside the bundle's scope, made to satisfy a mis-stated criterion.
- KEEP: the two out-of-scope reflows were reverted and must stay reverted. The `` `.bmad-loop/policy.toml` is tracked `` clause must stay on one physical line — guard 54's positive arm matches the path plus a few non-alphanumeric characters plus `is tracked`, and a wrap between them breaks it.

## Review Triage Log

### 2026-09-25 — Review pass
- verdicts: 28 findings — high 0, medium 10, low 14, false 4, maybe-false 0
- findings:
  - `[medium]` `[patch]` blind-hunter: the restamped provenance line reproduces DW-97 on this change's own lines — confirmed: `git cat-file -p 521ddb66:AGENTS.md` still reads "TypeScript on Node 22", "`tsc --noEmit` is the only real check" and "starting with `npm ci --no-audit --no-fund`", so the stamp again predates the text it labels. Fixed by splitting the commit: the content lands first, then the provenance line is restamped to that commit's SHA in a follow-up commit, so the stamped tree carries the verified block.
  - `[low]` `[reject]` blind-hunter: the spec restates the gate array's length ("63") against its own `Never` — real, but its only fix edits this build's spec, which triage rejects. Recorded here so the next reader knows the count is a 2026-09-25 measurement, not a maintained fact.
  - `[low]` `[reject]` blind-hunter: the Design Notes rationale for the caret form is wrong — confirmed, `semver.satisfies('26.0.0','^22.19.0 || ^24.0.0')` is `false`, so both candidate forms need an edit when 26 lands. Fix edits this build's spec; rejected on that rule. The decision of record ("Declare 22 and 24") is unaffected.
  - `[medium]` `[defer]` blind-hunter: DW-10's actual mechanism — `EBADENGINE` warn-only, no `engine-strict` — is untouched while the entry closes. Confirmed and deferred as `deferred[0]`.
  - `[medium]` `[defer]` blind-hunter: nothing in the gate reads `engines`, so the change adds a fourth unguarded mirror of the Node fact. Confirmed (`engines` appears zero times in `.bmad-loop/policy.toml`); grouped with the finding above as `deferred[0]`.
  - `[low]` `[patch]` blind-hunter: "Node 22 or 24" over-states the range — confirmed, `^22.19.0` rejects 22.0.0 through 22.18.x. Fixed: the line now reads "TypeScript on Node 22.19+ or 24 (`engines.node` in `package.json`)", 89 characters, no rewrap needed.
  - `[low]` `[reject]` blind-hunter: no command asserts the two manifests agree with each other, only that each prints the expected string. Fix edits this build's spec; rejected on that rule. The drift risk it names is deferred separately as `deferred[4]`.
  - `[low]` `[reject]` blind-hunter: the `git diff --stat` expectation omits the spec artifact this change adds. Fix edits this build's spec; rejected on that rule. Verified harmless — the artifact is untracked at the moment the command runs.
  - `[medium]` `[defer]` blind-hunter: the "only check" claim class is removed by hand and caught by nothing. Confirmed — guards 53/54/55 exit 0 on both the old and the new wording. Deferred as `deferred[2]`.
  - `[low]` `[defer]` blind-hunter: no operator-facing document states a Node prerequisite. Confirmed — `README.md`, `docs/`, `ops/README.md` and `sandbox/README.md` carry none. Pre-existing; deferred as `deferred[6]`.
  - `[low]` `[defer]` edge-case: installed packages declare Node ranges narrower than the new one, so `EBADENGINE` stays reachable inside 22 and 24. Confirmed from the lockfile (`posthog-node` `^20.20.0 || >=22.22.0`; 27 `@babel/*` at `^22.18.0 || >=24.11.0`). Pre-existing; deferred as `deferred[5]`.
  - `[false]` `[reject]` edge-case: the new range excludes Node 26, which `vitest@5.0.1` supports — factually true but not a defect: the 2026-09-24 decision of record says "Declare 22 and 24", so admitting 26 would deviate from the human decision this bundle exists to build.
  - `[medium]` `[defer]` edge-case: a `bmad-project-context` refresh regenerates the block and can revert all four corrections. Confirmed against `.bmad-loop/policy.toml:1991-1993` and `bmad-project-context/SKILL.md`'s Refresh section. Deferred as `deferred[2]`.
  - `[false]` `[reject]` edge-case: `ARCHITECTURE-SPINE.md:283` still restates `engines.node` as `>=22.19.0` — checked and refuted: that row sits under a heading reading "Seed. The code owns this from here." and "**Installed** — read from `package.json` and the tree on 2026-09-22", so it is a self-dated snapshot, not a current contract, and this change does not make it false.
  - `[low]` `[reject]` edge-case: the spec restates the array length four times (duplicate of the blind-hunter row above) — same rejection, fix edits this build's spec.
  - `[low]` `[reject]` edge-case: the spec says the block's edits landed in "two commits" — checked: `git log 47966e45..521ddb6 -- AGENTS.md` lists exactly one, `1796009`, the squashed integration of the two worktree commits DW-97 names. The spec inherited DW-97's pre-squash wording. Fix edits this build's spec; rejected on that rule.
  - `[low]` `[reject]` edge-case: the `git cat-file` acceptance criterion also passes for the SHA being replaced, so it cannot detect the staleness it was written to catch — confirmed, `47966e45:AGENTS.md` contains the markers too. Fix edits this build's spec; rejected on that rule. The underlying defect is the first row, which was patched.
  - `[medium]` `[defer]` verification-gap: the narrowed range is declared but no gate entry observes it and no runtime check enforces it. Arrived pre-verified with its own measurements; filed disposition `defer`. Deferred as `deferred[0]`.
  - `[medium]` `[defer]` verification-gap: the corrected AGENTS.md Node claim is reconciled against `package.json` by nothing. Arrived pre-verified; filed disposition `defer`. Deferred as `deferred[1]`.
  - `[low]` `[defer]` verification-gap (other): `@types/node` is pinned to the 22 major while 24 is now declared supported. Confirmed at `package.json:33`. Deferred as `deferred[3]`.
  - `[low]` `[defer]` verification-gap (other): npm does not validate the lockfile's root `engines` mirror against `package.json`. Confirmed by the layer's scratch-project demonstration. Deferred as `deferred[4]`.
  - `[medium]` `[defer]` intent-alignment (a): the intent's "every edit has to survive a refresh" points at the guard array, while the diff lives entirely in the regenerated bytes. Real; the array is append-only and out of this bundle's scope. Deferred as `deferred[2]`.
  - `[medium]` `[defer]` intent-alignment (b): the gate is blind to four of the five edits, so all 63 entries exit 0 identically on the pre- and post-change trees. Confirmed by running both. Deferred as `deferred[2]`.
  - `[medium]` `[defer]` intent-alignment (c): DW-10's expectation lives at the runtime-selection surface; the diff changes the text npm prints. Confirmed. Deferred as `deferred[0]`.
  - `[medium]` `[defer]` intent-alignment (d): the change replaces one hand-written cross-file claim with another of the same class. Confirmed. Deferred as `deferred[1]`.
  - `[medium]` `[patch]` intent-alignment (e): the acceptance criterion tests marker presence while DW-97 describes block identity, and the stamped tree carries none of the four corrected lines. Same root cause as the first row; fixed by the same split commit.
  - `[false]` `[reject]` intent-alignment (f): DW-94 and DW-95 say the array holds 55 entries while it now holds 63 — true of the ledger's historical text, which the orchestrator owns and this session must not edit. Not a defect in the diff, which correctly puts no count in AGENTS.md.
  - `[false]` `[reject]` intent-alignment (g): `_bmad-output/implementation-artifacts/` is not gitignored — confirmed, `git check-ignore` exits 1 and 39 files under it are tracked. Not a defect; recorded because it settles the artifact-only receipt question: this session's deliverables include tracked source files, so the receipt does not apply.

## Design Notes

`"^22.19.0 || ^24.0.0"` rather than `">=22.19.0 <25"`: the caret form states the two admitted majors directly, which is what the recorded decision says ("Declare 22 and 24"), and it excludes 23 without needing a second bound to be kept in step with a future 26.

The line-16 rewrite must not trade one false claim for another. It may not say or imply that some other command is the only check either; the shape the ledger asks for is negative and bounded — what `tsc --noEmit` reads — rather than a claim about the rest of the gate. Example of the intended shape:

```
  `include: ["src/**/*"]` — `tsc --noEmit` type-checks only the files that `include` names, so
  code outside `src/` is never type-checked.
```

The provenance line is a three-line HTML comment after the rewrap; guard 54 starts a new unit at an HTML comment and joins the continuation lines, so the split is safe. Keep the closing `-->` on the last line.

## Verification

**Commands:**
- `node -p "JSON.parse(require('fs').readFileSync('package.json','utf8')).engines.node"` -- expected: prints `^22.19.0 || ^24.0.0`
- `node -p "const r=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')).packages[''].engines.node; r"` -- expected: the same range as `package.json`
- `node -e "const s=require('semver'),r=JSON.parse(require('fs').readFileSync('package.json','utf8')).engines.node;for(const v of ['22.19.0','23.11.0','24.19.0','25.0.0'])console.log(v,s.satisfies(v,r))"` -- expected: `true`, `false`, `true`, `false` in that order (`semver` ships inside npm's own tree; if it cannot be resolved, state the four results from reading the range instead)
- `git diff --stat` -- expected: exactly `package.json`, `package-lock.json` and `AGENTS.md` changed; the lockfile hunk is the root `engines.node` line only
- A character-width (not byte-width) scan of the managed block, e.g. `node -e "const l=require('fs').readFileSync('AGENTS.md','utf8').split('\n');let f=false;l.forEach((s,i)=>{if(s.includes('<!-- /bmad:context -->'))f=false;if(f&&[...s].length>100)console.log(i+1,[...s].length);if(s.includes('<!-- bmad:context -->'))f=true})"` -- expected: the architecture-spine citation line only (pre-existing, single unbreakable path token, untouched here); the same scan on `baseline_revision`'s `AGENTS.md` reports lines 2, 43 and 56, so this change removes two of the three
- `grep -n -- '--no-audit' AGENTS.md` -- expected: no match
- `diff <(git cat-file -p <stamped-sha>:AGENTS.md | sed '2d') <(sed '2d' AGENTS.md)` and `git log --diff-filter=DR --name-only <stamped-sha>..HEAD` -- expected: no output from either; the stamped tree carries the verified block, and the refresh's delete/rename scan window is empty
- The full bmad-loop verify gate, every entry of `[verify].commands` in order starting with `npm ci --no-audit --no-fund` -- expected: all 63 exit 0

## Auto Run Result

Status: done

**Summary.** Narrowed `engines.node` from the open-ended `">=22.19.0"` to `"^22.19.0 || ^24.0.0"`, the two majors the 2026-09-24 decision declares and the only two `vitest@5.0.1` supports below 26, and corrected four claims inside AGENTS.md's `<!-- bmad:context -->` managed block: the Node version, the false "`tsc --noEmit` is the only real check" clause, the copied `npm ci --no-audit --no-fund` flags, and the over-long provenance line stamped at a SHA that predated the block. `ops/factory-start.sh`'s Node 24 pin, `docs/self-hosting-research.md:61`'s "one Node 24" and `sandbox/factory-sandbox.Dockerfile`'s `node:22-bookworm-slim` base are all left as they are — the recorded decision makes each accurate. `[verify].commands` was not touched.

**Files changed.**
- `package.json` — `engines.node` narrowed to `"^22.19.0 || ^24.0.0"`; `engines.npm` unchanged (DW-3, DW-10, DW-101).
- `package-lock.json` — the root `packages[""].engines.node` mirror regenerated with `npm install --package-lock-only`; the diff is that one line, no resolution moved.
- `AGENTS.md` — four managed-block corrections plus the wrap they required (DW-3, DW-65, DW-94, DW-95, DW-97).
- `_bmad-output/implementation-artifacts/spec-node-range-and-agents-md-claims.md` — this spec.

**Review findings.** 28 findings across four layers: 0 high, 10 medium, 14 low, 4 false, 0 maybe-false. Two entries patched, seven deferred, eight rejected.
- Patched (1 medium, 1 low): the restamped provenance line reproduced DW-97 — `521ddb66`, the HEAD the session started from, still carried all four uncorrected claims. Fixed structurally: a SHA written inside a commit can never name that commit, so the content landed in `4c176b4f84cdc16cd322c840c0d10d3d27d44596` and the stamp was written on top of it in a second commit. Verified: `4c176b4:AGENTS.md` is byte-identical to the final file apart from the provenance line, and the refresh's `git log --diff-filter=DR` window is empty. Also patched: "Node 22 or 24" became "Node 22.19+ or 24", because `^22.19.0` rejects 22.0.0 through 22.18.x.
- Deferred (3 medium, 4 low) — see frontmatter `deferred`: nothing in the gate reads `engines` and `EBADENGINE` stays warn-only, so DW-10's mechanism survives its closure; the corrected Node claim is reconciled against `package.json` by nothing; the "only check" claim class is still caught by nothing and the managed block is regenerable; `@types/node` is pinned to the 22 major; npm never validates the lockfile's `engines` mirror; several installed packages declare narrower ranges than the new one; no operator-facing document states a Node prerequisite.
- Rejected, each with its reason in the triage log: four findings whose only fix edits this build's spec (the restated array length, a wrong Design Notes rationale, a weak manifest-agreement check, a `git diff --stat` expectation that omits the spec artifact), one duplicate of the first of those, one about the spec inheriting DW-97's pre-squash "two commits" wording (`git log` shows one squashed commit, `1796009`), one about an acceptance criterion too weak to catch the staleness it targeted (the underlying defect was patched instead), and four `false`: excluding Node 26 is the decision of record, not a defect; `ARCHITECTURE-SPINE.md:283` is a self-dated 2026-09-22 snapshot under "The code owns this from here", not a current contract; the ledger's historical "55 entries" text is orchestrator-owned; and `_bmad-output/implementation-artifacts/` is not gitignored.

**Follow-up review recommended:** false. One `medium` and one `low` entry were patched on a first pass — below the two-medium threshold — and both patches were verified directly rather than by inference.

**Verification performed.**
- `engines.node` reads `^22.19.0 || ^24.0.0` in both `package.json` and the lockfile's root mirror.
- `semver.satisfies` against the declared range: 22.19.0 `true`, 23.11.0 `false`, 24.19.0 `true`, 25.0.0 `false`.
- Managed-block character widths: only the architecture-spine citation exceeds 100 (109, a single unbreakable path token, untouched here). At `baseline_revision` lines 2, 43 and 56 were over; this change removes two of the three and adds none.
- `grep -- '--no-audit' AGENTS.md`: no match. `[verify].commands` entry 1 still reads `"npm ci --no-audit --no-fund"` — the prose was loosened, the command was not.
- The stamped tree carries the verified block (byte-identical apart from the provenance line), and `git log --diff-filter=DR --name-only <stamped>..HEAD` is empty.
- The full bmad-loop verify gate, every entry of `[verify].commands` in order starting with `npm ci --no-audit --no-fund`: all 63 exit 0, guards 53, 54 and 55 included. Run twice — once after implementation, once after the review patches.

**Residual risks.** All four AGENTS.md corrections live inside a block `bmad-project-context` regenerates, and no gate arm matches the "only check" claim class or reconciles the Node claim against `package.json`, so a refresh can revert them and the gate would stay green; this is the repo's known one-story-lag problem and is deferred rather than solved here. The narrowed range is a declaration, not an enforcement: `npm ci` still exits 0 on an excluded major, so DW-10's original mechanism is closed on paper only. Both are recorded in frontmatter `deferred` with what would settle them.
