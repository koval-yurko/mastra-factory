---
scope: Repo restructure: directory layout, ops envelope, integration seams
purpose: Future-you reference — durable rules for where things live, re-readable in six months
altitude: repo/feature
updated: 2026-09-22T20:16
---

- (constraint) mastra build requires a literal new Mastra(...) in src/mastra/index.ts, enforced by the deployer checkConfigExport Babel plugin. The entry cannot be decomposed into packages or re-exported. [research ref 11]
- (constraint) tsconfig.json include is ["src/**/*"]. Any first-party TypeScript outside src/ is invisible to npm run check (tsc --noEmit), the only real check in the bmad-loop verify gate. [research ref 10]
- (constraint) varlock resolves .env and .env.schema from the repo root; npm run start is 'varlock run -- mastra start'. Root must remain the working directory.
- (constraint) LaunchAgent ai.mastra.factory pins WorkingDirectory to /Users/koval/dev/test/mastra-factory and factory-start.sh execs 'npm run start' after cd to that path. Repo root path and the start script name are load-bearing externally.
- (constraint) devDependencies are runtime dependencies (mastra CLI and varlock are needed by npm start) — no pruned production install is possible.
- (constraint) Factory's own design centralizes integration env reading in the deploy entry: 'the deploy entry reads that integration's env vars ONCE... No other module reads LINEAR_* env vars.' [research ref 4]
- (decision) [ADOPTED] Single npm package, one lockfile, no workspaces. Established by research 2026-09-22; the package-manager comparison dissolves with one package.
- (direction) Coaching path; purpose = future-you reference; scope = layout + ops envelope + integration seams.
- (decision) Organizing principle: BY SUBJECT for operator artifacts (user's call, against the facilitator's by-consumer lean). Rationale accepted: by-consumer was internally inconsistent (sandbox Dockerfile in docker/ while docker-compose.yml sits at root), and each subject folder does hold a real homeless artifact (GitHub permission registry, Linear scopes, Slack manifest).
- (decision) Source of truth for operational artifacts: FILES CANONICAL, doc links out. docs/Self-hosting research.md keeps prose, decisions and the risk register but replaces fenced code blocks with path references. Prevents: two copies of a plist/Dockerfile drifting apart.
- (question) Does the two-plane refinement (operator plane by subject, code plane bounded by src/ and tsc reach) hold, or does the user want subject grouping inside src/ too?
- (direction) Facilitator pushed back on by-subject re: risk of .ts landing outside src/ and escaping tsc; resolved by a two-plane paradigm rather than reversing the choice.
- (constraint) Factory-skills override path is hard-coded, not a choice: resolveLocalFactorySkillsPath() probes <cwd>/src/mastra/public/factory-skills, <cwd>/public/factory-skills, <cwd>/factory-skills — local wins per-file over bundled. Only the six FACTORY_SKILL_NAMES are recognized. src/mastra/public/factory-skills/ is the one path valid under both cwd variants. [node_modules/@mastra/factory/dist/workspace.js:30-45]
- (decision) Paradigm: TWO PLANES with a shared subject vocabulary. Operator plane = subject folders holding artifacts a human or provider console consumes, never .ts. Code plane = everything under src/, grouped by the SAME subjects (src/mastra/integrations/<name>/, src/mastra/rules/<provider>.ts). Prevents: .ts landing outside src/ where tsc cannot see it, and two different vocabularies for the same subject.
- (decision) Entry shape: FULL EXTRACTION to src/mastra/config/ (user's call). Facilitator flagged that this makes index.ts structurally unlike upstream so future template updates become manual reconciliation; user accepted that cost with the tradeoff stated.
- (decision) Mitigation for the extraction cost: record the upstream template provenance (package version / template commit) that index.ts was forked from, so a future template update is a three-way diff rather than archaeology.
- (event) Facilitator corrected its own research-doc recommendation: src/mastra/config/ does NOT shrink the template diff — it reduces line count but destroys mergeability, because upstream index.ts already contains the env-reading being extracted.
- (question) Open: who may read process.env once config/ exists, and whether .env.schema or subject READMEs are normative for env keys.
- (decision) Env reads: NO location restriction — process.env may be read anywhere under src/mastra/ (user's call, lowest ceremony; the code plane is ~4 files today). Facilitator strengthening, rejectable at review: each env key must have exactly ONE read site, so grep for a key lands in one place and the .env.schema §11 trap table stays findable.
- (decision) Env key truth: SPLIT BY NATURE. .env.schema is normative for validation, types and @public/sensitive marking. Each apps/<subject>/README.md is normative for what the value must contain and how to obtain it from the provider console. Prevents: two competing definitions of the same key.
- (assumption) [ASSUMPTION] The 'exactly one read site per key' rule is the facilitator's addition, not the user's stated choice — flag in review.
- (question) Deferred candidate: revisit the env-read location rule if first-party code under src/mastra/ grows past a handful of modules.
- (event) Reviewer gate run: lint_spine.py clean (0 findings after placeholder-notation fix). Two configured lenses run inline rather than as independent subagents — weaker than the gate prescribes; disclosed to user.
- (event) Adversarial lens found 6 divergences, all closed: D1 two legal homes for a new subject (AD-3 namespace rule + closed root set); D2 docker-compose.yml unassigned (AD-3 root allowlist); D3 AD-6 missed non-apps subjects (generalized to owning subject README); D4 AD-8 text said 'deltas' but decision was full extraction (reworded); D5 non-TS first-party code escaped AD-4 (broadened + ops/*.sh carve-out); D6 vendored/tooling trees belonged to no plane (new AD-13).
- (event) Version lens: @mastra/docker and @mastra/auth-better-auth are NOT installed — their versions were inherited from the self-hosting doc, not reality-checked this run. Stack table split into Installed vs Planned-not-yet-resolved.
- (event) spine finalized
- (direction) Open question carried to handoff: AD-7 (one read site per env key) is the facilitator's strengthening of the user's 'no restriction' choice and is rejectable.
