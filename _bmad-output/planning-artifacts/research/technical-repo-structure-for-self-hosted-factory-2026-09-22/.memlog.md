---
topic: Repo structure for the self-hosted Factory
type: technical
decision: Monorepo vs nested folders as GitHub/Linear/Slack apps, Better Auth and the sandbox image land
preset: standard
updated: 2026-09-22T19:52
---

- (decision) Redirected from web-research fan-out to primary-source package inspection: the decisive question ('does an own GitHub/Linear/Slack App need code?') is answered by the shipped @mastra/factory@0.15.0 artifact in node_modules, not by search. Web adds nothing authoritative here.
- (source) Primary sources read this run: @mastra/factory@0.15.0 dist/ (166 JS modules, 6.3 MB) incl. integrations/{github,linear,slack}; src/mastra/index.ts (repo entry); tsconfig.json; docs/Self-hosting research.md v6 (project doc, context only).
- (claim) ref=[C1] status=verified class=version pub=2026-09 — All three integrations ship complete in @mastra/factory@0.15.0 (github 20 modules/235KB, linear 8/52KB, slack 6/83KB)
- (claim) ref=[C2] status=verified class=version pub=2026-09 — src/mastra/index.ts already constructs GithubIntegration, LinearIntegration and SlackIntegration from env vars; zero first-party modules needed
- (claim) ref=[C8] status=verified class=version pub=2026-09 — No Slack app manifest ships in the package; the manifest YAML is the only authored artifact among the three apps
- (claim) ref=[C10] status=verified class=version pub=2026-09 — tsconfig include is src/**/* only, so TS outside src/ is invisible to the npm run check verify gate
- (claim) ref=[C11] status=verified class=version pub=2026-09 — mastra build requires a literal new Mastra(...) in the entry file per the deployer checkConfigExport Babel plugin; the entry cannot be decomposed into packages
- (decision) Verdict: no monorepo, no workspaces, no per-app folders. Single package + four flat sibling dirs (docker/, ops/, docs/apps/, optional src/mastra/config/). Stay on npm - package-manager comparison dissolves with one package.
- (event) Citation refs normalized from C<n> to numeric <n> across research.md and digests so recon_kit citations can verify them mechanically; memlog claim lines logged earlier as ref=[C1..C11] map 1:1 onto the numeric refs.
