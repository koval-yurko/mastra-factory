---
topic: Self-hosted 24/7 Mastra Factory on one Mac: own GitHub/Linear/Slack apps, Better Auth, Docker sandboxes, Cloudflare ingress, launchd supervision, and the two-plane repo layout that houses the operational artifacts
updated: 2026-09-22T20:33
---

- (event) Create run, interactive. Yurii chose multi-source input: ARCHITECTURE-SPINE.md + technical research.md + docs/Self-hosting research.md; slug self-hosted-factory.
- (decision) Mixed multi-source input, so bmad-spec does the sorting itself (three-lens load-bearing test per claim) rather than trusting an authored split.
- (decision) Rich input -> express-by-extraction, no elicitation. Only genuinely unresolved items become open_questions.
- (decision) ARCHITECTURE-SPINE.md and docs/Self-hosting research.md are ADOPTED companions, not absorbed sources: both are upstream-owned, both remain canonical (AD-5 files are canonical; spine cites the research doc by stable section anchors). Duplicating their ADs/tables into spec-authored companions would create the exact two-copies drift AD-5 and AD-6 forbid.
- (decision) technical research.md IS fully absorbed into sources[]: its load-bearing claims (no monorepo, the two hard constraints, npm stays, revisit triggers) are all restated canonically in the spine. Downstream does not need to read it.
- (decision) research.md §4's layout proposal (docker/ + ops/ + docs/apps/, grouped by consumer) is SUPERSEDED by spine AD-3 (apps/<subject>/ + sandbox/ + ops/, grouped by subject). Spine is final and newer; research.md is draft and is a spine source. No live either/or - not surfaced to user.
- (decision) One spec, not two. The deployment build-out and the repo restructure share one Why and one success signal, and the restructure exists to house the deployment's operational artifacts.
- (decision) Two-phase ordering is a constraint, not a conflict: research §4 Ordering says restructure AFTER the §8 build order verifies end-to-end; §8 step 5 lands the DockerSandbox + Better Auth edits in index.ts, and AD-8's total extraction into src/mastra/config/ migrates them afterward.
- (capability) CAP-1 Docker sandboxes on this machine via Colima, one long-lived container per session, hard-capped.
- (capability) CAP-2 Self-hosted identity with orgs via Better Auth deferred-instance mode into the same Postgres; registration closed before public exposure.
- (capability) CAP-3 Own GitHub App drives issue -> merged PR end to end.
- (capability) CAP-4 Own Linear OAuth app provides issue intake and @-mention.
- (capability) CAP-5 Own Slack app (operator-authored manifest - Mastra ships none) provides channel work and account linking.
- (capability) CAP-6 Public HTTPS ingress via Cloudflare Tunnel with no inbound ports.
- (capability) CAP-7 Unattended 24/7 supervision: LaunchAgents, wait-for-socket wrapper, log rotation, power settings.
- (capability) CAP-8 Every operational artifact exists as exactly one real file in the two-plane layout, replacing the fenced blocks in docs/Self-hosting research.md.
- (capability) CAP-9 The Mastra entry is fully extracted into src/mastra/config/ with recorded template provenance.
- (capability) CAP-10 One Postgres 18 + pgvector instance is the single store for app tables and auth/org tables; no Redis.
- (note) Zero dependency on Mastra's platform is the anchor of Why: a stray env var (MASTRA_SHARED_API_URL, MASTRA_PLATFORM_*, E2B_API_KEY, SANDBOX_PROVIDER, WORKOS_*) silently reaches back, so 'must stay unset' is a constraint, not a note.
- (constraint) Entry indivisible: src/mastra/index.ts must export 'mastra' built by a literal new Mastra(...) in that file - deployer's checkConfigExport Babel plugin reads the source (AD-2).
- (constraint) All first-party program code under src/ only; tsconfig include stays ['src/**/*'] because tsc --noEmit is the only real verify-gate check. Carve-out: ops/*.sh, kept to process supervision and file placement (AD-4).
- (constraint) One npm package, one root package.json + package-lock.json, no workspaces, npm stays - a package-manager switch is a full dependency re-resolution and the posture is no-backups/one-way bumps (AD-1).
- (constraint) Two planes, one subject vocabulary; provider/app subjects under apps/, host infrastructure at root, root a closed set; root allowlist is tool-fixed files only (AD-3).
- (constraint) Factory-skill overrides only at src/mastra/public/factory-skills/<name>/SKILL.md, only the six bundled names resolve, per-file override; .agents/skills/ is hash-locked and must not be hand-edited (AD-9, AD-13).
- (constraint) Single machine, single process, no Redis: REDIS_URL unset, redis service removed from docker-compose.yml, workers in-process. Any multi-replica or shared-queue design contradicts this and is a conflict to surface (AD-12).
- (constraint) Env-key truth split: .env.schema is normative for validation/types/@public marking; the owning subject's README is normative for what the value must contain and how to obtain it. One owning subject per key (AD-6).
- (constraint) process.env.X for any X read at exactly one site in first-party code (AD-7) - carried as the spine's one open question; facilitator addition, not user-stated.
- (constraint) Files canonical, prose links out: one real file per operational artifact; docs/ references by path and marks code blocks non-normative (AD-5).
- (constraint) Repo root path and npm script names are an external contract: npm run start stays the production entry and must work with repo root as cwd; renaming or moving requires updating the plist, factory-start.sh and the newsyslog conf in the same change (AD-11).
- (constraint) MASTRA_HOST must be 127.0.0.1 - unset binds all interfaces including LAN. TLS is Cloudflare's job; MASTRA_HTTPS_KEY/_CERT stay unset.
- (constraint) signUpEnabled defaults true: the account is created on loopback and registration closed before the tunnel is switched on.
- (constraint) FACTORY_CREDENTIAL_ENCRYPTION_KEY must be set before any credential is stored, or provider keys and OAuth tokens persist as plaintext. GITHUB_APP_WEBHOOK_SECRET must be stable forever - it is also the primary OAuth-state signer.
- (constraint) No backups; the Postgres volume is the only copy. Every dependency bump is one-way, changelog first.
- (constraint) FileVault on: no unattended recovery from an unplanned reboot. Planned reboots go through fdesetup authrestart. Accepted.
- (constraint) Findings are pinned to @mastra/factory@0.15.0; a major bump requires re-verifying the integration contract and the extension seams.
- (constraint) Secrets never in the repo: .env gitignored, .env.schema/.env.example carry key names and shapes only.
- (constraint) Sandbox image tags are date-stamped, never latest, so a bad image is a FACTORY_SANDBOX_IMAGE rollback.
- (decision) Non-goals taken from the spine's Deferred table plus the self-hosting doc's explicit exclusions: monorepo/workspaces, task orchestration, CI beyond the local gate, backup/restore, multi-tenant GitHub Apps, hosted identity (WorkOS/platform), cloud sandboxes (E2B/Platform VMs), server-terminated TLS, unattended reboot recovery.
- (decision) Spec-authored companion extension-seams.md: the four-seam catalog from research §2 is the one load-bearing table no adopted file carries in full - AD-10 omits the Slack adapterOptions seam - so it is preserved here rather than lost.
- (decision) Spec-authored companion brownfield.md: the structural seed is entirely aspirational (src/ holds only index.ts; no apps/, sandbox/, ops/, .env). Verified against the tree 2026-09-22. Downstream builders need the real starting point, not the target, to sequence work.
- (assumption) docs/Self-hosting research.md is the file the sprint-plan memory calls SELF_HOSTING_RESEARCH.md - same content, renamed.
- (assumption) Brownfield facts in brownfield.md are observed from the working tree on 2026-09-22, not stated by any of the three sources.
- (assumption) factory.kovalchuk.win on an Active kovalchuk.win zone remains the intended public origin.
- (question) Does AD-7 (one read site per env key) survive review? Facilitator addition, not user-stated; reject and location and count both go unrestricted. Non-blocking. Owner: Yurii.
- (question) Is substantial custom logic intended (per-tenant GitHub Apps, a new FactoryIntegration, heavy rule overrides)? Nothing in the repo indicates it; the answer decides whether the extension seams matter at all or stay theoretical.
- (question) Do the dependencies for the ruled-out paths come out of package.json - @mastra/auth-workos, @mastra/e2b, @mastra/libsql, @mastra/redis-streams, @mastra/platform-workspace? The sources only require their env vars stay unset and the redis compose service be deleted; removing the packages is neither mandated nor forbidden.
- (question) Template residue: delete pnpm-workspace.yaml (AD-1 calls it non-normative, research §5 says delete-or-leave)? Unresolved either/or in the sources.
- (question) Rename docs/Self-hosting research.md to a space-free path? The space is a shell-quoting footgun and the spine makes this path a heavily cited stable anchor - a rename touches every citation.
- (decision) research.md §4's 'src/mastra/config/ is optional and reversible' is SUPERSEDED by spine AD-8: extraction is mandatory and total, per module.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 + 8): PASS. All 10 capabilities carry intent + success; 10 stable unique IDs CAP-1..CAP-10; 11 non-goals; success signal is a demonstrable three-source-issue-to-merged-PR plus a logout/login survival test. Noted and accepted: CAP-1 and CAP-9 name Docker/Colima and the config/ extraction in their intents - normally a rule-2 HOW, but both are settled constraint-level decisions (AD-12, AD-8), not implementation latitude.
- (event) Self-validate pass 2 (preservation, Spec Law 7): PASS. Walked all three sources claim by claim. docs/Self-hosting research.md §§0-11, ARCHITECTURE-SPINE.md AD-1..AD-13 + conventions + stack + seed + deferred + open question, and research.md §§1-7 all land in the kernel, an adopted companion, or a spec-authored companion. All four companion paths and the one source path verified to resolve from the spec folder.
- (event) Wrapper-only content deliberately dropped: research.md's conversational framing ('your instinct is correct', 'the restructure impulse is not wrong'), its ~30-minutes migration estimate, its §7 meta-note about redirecting away from a planned web fan-out, and its source-citation table (traceability stays in the source file itself). Also dropped: the two spine review files - process metadata recording how the spine was produced, and both verdicts are already incorporated into the final spine.
- (event) Spec finalized: SPEC.md + extension-seams.md + brownfield.md written; ARCHITECTURE-SPINE.md and docs/Self-hosting research.md adopted as companions; technical research.md recorded as a fully-absorbed source.
